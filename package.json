{
    "name": "safe-fs-write-stream",
    "version": "1.0.0",
    "scripts": {
        "test": "istanbul --report=html --report=text-summary --report=cobertura --report=lcov cover _mocha"
    },
    "devDependencies": {
        "chai": "^3.5.0",
        "istanbul": "^0.4.3",
        "mocha": "^2.4.5",
        "proxyquire": "^1.7.4"
    }
}
